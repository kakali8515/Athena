export default {

    getCertificate(state, value) {
       state.certificate = value;
    },

    getToken(state, value) {
      state.token = value;
   },

   getUemail(state, value) {
      state.email = value;
   },
 
 }